NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1
# Optional: enables full EVM transaction history (Etherscan v2 multi-chain API); without it, EVM history shows a warning instead of a partial/misleading result
NEXT_PUBLIC_ETHERSCAN_API_KEY=
